// This file is used to run the game loop
using Ship = Ship.Ship;
using Playerboard = PlayerBoard.PlayerBoard;

namespace GameLoop
{
    public class GameLoop
    {
        private Playerboard player = new Playerboard(); // human player
        private Playerboard opponent = new Playerboard(); // ai player

    }
}